// escribe tu función acá

// código de prueba
let words = ["beta", "alfa", "Arbol", "gama"]
let result = words.filter((word) => word[0] == 'a' || word[0] == "A")
console.log(result) // ["alfa", "Arbol"]
